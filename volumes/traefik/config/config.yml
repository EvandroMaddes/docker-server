http:
  
  #        #        #
  # ROUTERS SECTION #
  #        #        #
  routers:
    dashboard:
      middlewares:
      #  - auth
      service: api@internal
      # To set the value of a rule, use backticks ` or escaped double-quotes \".
      rule: "Host(`{{env "DOMAIN_NAME"}}`)"
      tls:
        # nomepuntocom defined in traefik.yml
        # You can set SANs (alternative domains) for each main domain.
        # Every domain must have A/AAAA records pointing to Traefik.
        # Each domain & SAN will lead to a certificate request.
        # Certificates will be available to other routers using the specified domain
        certResolver: nomepuntocom
        domains:
          - main: "{{env "DOMAIN_NAME"}}"
            sans:
              - "*.{{env "DOMAIN_NAME"}}"
    whoami:
      middlewares:
      #  - auth
      entryPoints:
        - "websecure"
      service: whoami-sensor
      # name are handled on dns of the router using CNAME, they are redirected on domain name and handle by 
      # alternative rule using path over host rule: Host(`{{env "DOMAIN_NAME"}}`) && PathPrefix(`/whoami/`) host 
      rule: "Host(`{{env "DOMAIN_NAME"}}`) && PathPrefix(`/sensor`)"
      # TLS section will terminate the TLS connections, meaning that it will send decrypted data to the services.
      # the passthrough option can be specified to set whether the requests should be forwarded "as is", keeping all data encrypted.
      tls: {}
    filebrowser:
      entryPoints:
        - "websecure"
      service: filebrowser-service
      rule: "Host(`filebrowser.{{env "DOMAIN_NAME"}}`)"
      tls: {}
    homepage:
      entryPoints:
        - "websecure"
      service: homepage-service
      rule: "Host(`homepage.{{env "DOMAIN_NAME"}}`)"
      tls: {}
    emby:
      entryPoints:
        - "websecure"
      service: emby-service
      rule: "Host(`emby.{{env "DOMAIN_NAME"}}`)"
      tls: {}
    sonarr:
      entryPoints:
        - "websecure"
      service: sonarr-service
      rule: "Host(`sonarr.{{env "DOMAIN_NAME"}}`)"
      tls: {}
    immich:
      entryPoints:
        - "websecure"
      service: immich-server-service
      rule: "Host(`immich.{{env "DOMAIN_NAME"}}`)"
      tls: {}

  
  #         #        #      
  # SERVICES SECTION #
  #         #        #
  services:
    whoami-sensor:
      loadBalancer:
        servers:
          - url: "http://{{env "WHOAMI_INTERNAL_IP"}}:{{env "WHOAMI_PORT"}}"
    filebrowser-service:
      loadBalancer:
        servers:
          - url: "http://{{env "FILEBROWSER_INTERNAL_IP"}}:{{env "FILEBROWSER_PORT"}}"
    telegram-bot-service:
      loadBalancer:
        servers:
          - url: "http://{{env "TELEGRAM_BOT_INTERNAL_IP"}}:{{env "TELEGRAM_BOT_PORT"}}"
    homepage-service:
      loadBalancer:
        servers:
          - url: "http://{{env "HOMEPAGE_INTERNAL_IP"}}:{{env "HOMEPAGE_PORT"}}"
    emby-service:
      loadBalancer:
        servers:
          - url: "http://{{env "EMBY_INTERNAL_IP"}}:{{env "EMBY_PORT"}}"
    sonarr-service:
      loadBalancer:
        servers:
          - url: "http://{{env "SONARR_INTERNAL_IP"}}:{{env "SONARR_PORT"}}"
    qbittorrent-service:
      loadBalancer:
        servers:
          - url: "http://{{env "QBITTORRENT_INTERNAL_IP"}}:{{env "QBITTORRENT_PORT"}}"
    jackett-service:
      loadBalancer:
        servers:
          - url: "http://{{env "JACKETT_INTERNAL_IP"}}:{{env "JACKETT_PORT"}}"
    immich-server-service:
      loadBalancer:
        servers:
          - url: "http://{{env "IMMICH_SERVER_INTERNAL_IP"}}:3001"

  
  #            #        #
  # MIDDLEWARES SECTION #
  #            #        #
  middlewares:
    auth:
      basicAuth:
        users: admin:$$apr1$$lTm6sC9j$$p3R/Qzdp35u2iME.HhVCn/
        headerField: "X-WebAuth-User"
